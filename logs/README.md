Runtime workflow logs are written here.

- `logs/bot/` contains bot worker logs.
- `logs/scale/` contains scale worker logs.

Each process startup clears its own folder and starts a fresh log session.
